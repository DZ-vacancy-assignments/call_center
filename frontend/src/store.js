import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

// This is not used, I turned it off.
const store = new Vuex.Store({
  strict: true,
  state: {},
  mutations: {},
  actions: {}
})

export default store
